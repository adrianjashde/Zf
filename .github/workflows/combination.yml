name: blank

on:
  push:

jobs:
  breakthrough:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        command: ["wrist", "tension", "motion", "charity", "sunset"]
        accomplish: ["fruitful", "aboard", "plot", "enrich"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Up
